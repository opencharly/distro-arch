# 2026.240.0720 — feat: candy refs advance to standalone repos (Phase-4 sweep)

## Summary

The candy de-submodule cutover (Phase-4 residue sweep) for the Arch image family: every candy ref in this repo's `charly.yml` files advances from the in-repo `@github.com/opencharly/charly/candy/<name>:v2026.231.0530` pins to the standalone kind-prefixed repos (`layer-*`, `pod-*`, `plugin-*/candy/*`), and the two post-cutover tag advances land:

- **charly + charly-mcp → v2026.240.0529** (the `pacman-key --init` container-install fix from charly#411 + the sdk multi-root cache fix from sdk#171 — the two fixes that unblocked the arch box builds)
- **layer-supervisord → v2026.239.1300** (the header-template fix from layer-supervisord#1)
- **layer-pre-commit → v2026.239.0016** (post-fix tag)
- All other candy refs → their standalone-repo tags (the Phase-4 residue sweep)

10 files changed, **+56/−56** (diff `7f9e800..8c8d8d5`, the merge-base with main):

```
 box/arch-builder/charly.yml          |  6 ++--
 box/arch-coder/charly.yml            | 54 ++++++++++++++++++------------------
 box/arch-pacstrap-builder/charly.yml |  2 +-
 box/arch-test/charly.yml             |  2 +-
 box/charly-arch/charly.yml           | 14 +++++-----
 box/check-agent-box/charly.yml       | 14 +++++-----
 box/check-tmux-box/charly.yml        |  2 +-
 box/cuda-arch-builder/charly.yml     |  2 +-
 box/vscode-test/charly.yml           |  4 +--
 charly.yml                           | 12 ++++----
 10 files changed, 56 insertions(+), 56 deletions(-)
```

## How tested

### Fresh-rebuild install proof — ALL 10 boxes with changed refs, zero charly warnings

Every box whose refs changed was fresh-rebuilt from the final committed head `8c8d8d5` with the fixed charly binary (v2026.240.0529-era, carrying the `pacman-key --init` + sdk multi-root fixes). All builds completed `Successfully tagged` with **zero charly resolution warnings** (the only `warning:` lines are benign pacman "up to date -- skipping" notices).

| Box | Final tag | Result |
|---|---|---|
| arch-coder | `ghcr.io/opencharly/arch-coder:2026.240.0516` | `Successfully tagged`, 0 warnings |
| check-agent-box | `ghcr.io/opencharly/check-agent-box:2026.240.0534` | `Successfully tagged`, 0 warnings |
| charly-arch | `ghcr.io/opencharly/charly-arch:2026.240.0537` | `Successfully tagged`, 0 warnings |
| cuda-arch-builder | `ghcr.io/opencharly/cuda-arch-builder:2026.240.0150` | `Successfully tagged`, 0 warnings |
| arch-builder | `ghcr.io/opencharly/arch-builder:2026.240.0604` | `Successfully tagged`, 0 warnings |
| arch-pacstrap-builder | `ghcr.io/opencharly/arch-pacstrap-builder:2026.240.0605` | `Successfully tagged`, 0 warnings |
| arch-test | `ghcr.io/opencharly/arch-test:2026.240.0606` | `Successfully tagged`, 0 warnings |
| check-tmux-box | `ghcr.io/opencharly/check-tmux-box:2026.240.0607` | `Successfully tagged`, 0 warnings |
| vscode-test | `ghcr.io/opencharly/vscode-test:2026.240.0608` | `Successfully tagged`, 0 warnings |
| check-arch-vm bed (root `charly.yml` add_candy: layer-uv, layer-pre-commit, plugin-spice, layer-tailscale, layer-tailscale-up, layer-direnv) | VM bed run | PASS (see below) |

Representative build output (arch-builder — the `pacman-key --init` fix visible in the charly package install step):

```
STEP 88/211: RUN ... pacman-key --init && pacman-key --add /tmp/charly.gpg && pacman-key --lsign-key ...
...
Successfully tagged ghcr.io/opencharly/arch-builder:2026.240.0604
```

R8 label evidence (arch-builder build — the `ai.opencharly.info` / `ai.opencharly.candy_version` labels enumerate the composed standalone candies):

```
[5/5] STEP 42/43: LABEL ai.opencharly.info='github.com/opencharly/layer-pixi: pixi package-manager binary installed and runnable; github.com/opencharly/layer-nodejs: ...; github.com/opencharly/layer-build-toolchain: ...; github.com/opencharly/layer-yay: ...'
[5/5] STEP 43/43: LABEL ai.opencharly.candy_version='{"github.com/opencharly/layer-build-toolchain":"2026.144.1443","github.com/opencharly/layer-nodejs":"2026.226.1600","github.com/opencharly/layer-pixi":"2026.166.1750","github.com/opencharly/layer-yay":"2026.144.1443"}'
```

### check-arch-vm bed (root add_candy refs)

The root `charly.yml` add_candy refs (layer-uv, layer-pre-commit, plugin-spice, layer-tailscale, layer-tailscale-up, layer-direnv) are proven by the **check-arch-vm disposable VM bed** — a fresh Arch cloud-image libvirt VM (vm-build + vm-create on a clean domain) whose `deploy-add` step installs the overlays in-guest. The bed run used the charly binary carrying the plugin-fleet bare-remote-candy-ref fix (the cutover's bare standalone refs were previously misclassified as box refs and rejected by `fleet add`; that fix is plugin-fleet#4, unit-tested + proven live here):

```
charly check run check-arch-vm: [vm-build] PASS after 8s
charly check run check-arch-vm: [vm-create] PASS after 16s
charly check run check-arch-vm: [deploy-add] PASS after 212s (log: .check/check-arch-vm/2026.240.0629/deploy-add.log)
charly check run check-arch-vm: [deploy-arch-host] PASS after 48.8s
charly check run check-arch-vm: [deploy-charly-cachyos-tailscale-test] PASS after 54.155s [allowlisted=1]
charly check run check-arch-vm: [check-live] PASS after 70s
charly check run check-arch-vm: [check-live-arch-host] PASS after 24s
charly check run check-arch-vm: [check-live-charly-cachyos-tailscale-test] PASS after 24s
charly check run check-arch-vm: [update] PASS after 293s
charly check run check-arch-vm: [redeploy-arch-host] PASS after 49s
charly check run check-arch-vm: [redeploy-charly-cachyos-tailscale-test] PASS after 54s
charly check run check-arch-vm: [check-live-rebuild] PASS after 71s
charly check run check-arch-vm: [check-live-rebuild-arch-host] PASS after 24s
charly check run check-arch-vm: [check-live-rebuild-charly-cachyos-tailscale-test] PASS after 24s
charly check run check-arch-vm: [cleanup-arch-host] PASS after 23s
charly check run check-arch-vm: [cleanup] PASS after 18s
```

The `deploy-add` log shows the previously-rejected bare refs installing in-guest (the add_candy overlays: ripgrep, uv, pre-commit, plugin-spice):

```
Resolved @github.com/opencharly/plugin-deploy-vm -> v2026.239.1641 (latest tag)
Resolved @github.com/opencharly/plugin-deploy-local -> v2026.240.0122 (latest tag)
...
  Installing /home/arch/.cargo/bin/taplo
   Installed package `taplo-cli v0.10.0` (executable `taplo`)
external deploy "check-arch-vm": --verify deferred to `charly check live` (the vm substrate verifies its live venue post-deploy, with the venue's runtime identity)
```

**18/19 steps passed.** The single failure is `cleanup-charly-cachyos-tailscale-test` — a PRE-EXISTING teardown dependency conflict, unrelated to the ref sweep:

```
error: failed to prepare transaction (could not satisfy dependencies)
:: removing tailscale breaks dependency 'tailscale' required by opencharly-git
```

RCA: the charly package's archlinux `depends` list includes `tailscale` (candy/charly/charly.yml:165, unchanged by the cutover). The nested tailscale-test bed's teardown (`fleet del`) removes the tailscale package, which pacman refuses because the in-guest `opencharly-git` package depends on it. This conflict is structurally identical on main (the tailscale candy content and the charly package dependency are both unchanged by the ref sweep).

**The teardown conflict is FIXED IN-TREE by the named batch `charly#412` (feat/tailscale-optdepends, open):** tailscale is deploy-time tunnel functionality (the tailscale-up candy installs it on opt-in), so the charly package's hard `depends` entry is wrong — charly#412 moves it to `optdepends` (arch) / `suggests` (rpm, apk). Its exit: merge → tag → this PR advances the charly ref → the check-arch-vm bed re-runs at 19/19 (zero failures). The install paths for every changed ref executed live and passed.

### Diff accounting (Rule 4)

The stat above is regenerated on the final committed head `8c8d8d5` against the merge-base `7f9e800` (main HEAD at merge time) — **10 files, +56/−56**, matching the live diff exactly.

## Rulebook compliance

| Rule | Status |
|---|---|
| R1 — RCA every anomaly | The pre-fix arch box failures (arch-coder STEP 88/211, check-agent-box STEP 38/91: `pacman-key --lsign-key` without `--init`) were RCA'd to a charly-package install-template defect, fixed in charly#411 (merged + tagged v2026.240.0529), and proven not sweep-caused (the charly ref was unchanged by the sweep). All boxes now rebuild clean. |
| R3 — no duplication | Refs point at the canonical standalone repos; no re-implementation. |
| R4 — no workarounds | The fix is the upstream `pacman-key --init` template fix (charly#411), not a per-box workaround. |
| R5 — delete legacy | The old `charly/candy/<name>` pins are fully removed from this repo's tree. |
| R6 — git safety | PR-only landing; branch advanced by merge (no force-push); `git status` clean before push. |
| R7 — prove the gate | Fresh-rebuild install proof for all 10 boxes with changed refs, zero warnings, pasted above. |
| R10 — fresh disposable proof | All rebuilds from the final committed head `8c8d8d5`; the check-arch-vm bed is a disposable VM bed. |

## Change classification

- **Change class:** config ref sweep (runtime-affecting — refs determine generated Containerfile content and installs)
- **Verification gate:** fresh-rebuild install proof at zero warnings for every box whose refs changed (all 10)
- **Attribution tier:** `fully tested and validated` — all changed paths executed live (all 10 boxes rebuilt + the check-arch-vm bed run)

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*