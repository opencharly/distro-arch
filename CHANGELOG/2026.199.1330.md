# 2026.199.1330 — agent transport R10 beds

- Add disposable Arch beds for the typed tmux terminal provider and the full
  agent control plane.
- Exercise deployment→gRPC→tmux, SSH→gRPC→tmux, nested deployment→SSH routes,
  Pi runtime/TUI integration, federation, capability discovery, durable
  transcripts, cleanup, update, and rebuild behavior through Charly commands.
- Preserve stage-labelled JSONL evidence for critical terminal runs so failures
  identify the transport stage and protocol frames without ad-hoc container
  inspection.
- Assert after the real Claude terminal session that auto-update remains
  disabled and the declared package pin, native binary, and version command are
  unchanged before the bed proceeds to its fresh-rebuild phase.
- Pace the Pi-TUI bed check on observable terminal state through the typed
  terminal:tmux API (detached launch, engine-retried snapshot readiness probes,
  literal input plus allowlisted keys, reattached exit witness) instead of fixed
  wall-clock sleeps; hoist the Claude pin into one variable; document the
  bed-runner host `node` requirement; give the credential-projection group an
  honest iterate prompt.
