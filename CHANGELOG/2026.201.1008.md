# 2026.201.1008 — Post-#146 reconcile: adopt charly v2026.201.0706 pins

Every `@github.com/opencharly/charly/...` pin moves from `v2026.183.1359` to
`v2026.201.0706` via `charly box reconcile --remote` (issue
opencharly/charly#158). The pre-#146 pins fetched protocol-v1 candy content;
under a protocol-[2] host binary every box build whose candies load
out-of-process builder plugins failed `Plugin version: 1, Client versions:
[2]`.

Evidence: `charly box validate` exit 0; `box build arch-test` passes
(`ghcr.io/opencharly/arch-test:2026.201.0740`). `box build arch-coder`
passes the builder-plugin phase (the issue's failure point) and stops only at
the `candy/charly` release-package download — no GitHub release exists
because the release-packages workflow runs are queued on an unavailable
self-hosted runner (operator infrastructure, unrelated to the pins).

Assisted-by: Kimi Code (Moonshot K3; analysed on a live system)
