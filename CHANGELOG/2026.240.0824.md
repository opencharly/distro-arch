# 2026.240.0824 — chore: advance charly refs to v2026.240.0759 (tailscale optdepends fix)

## Summary

Advance the charly + charly-mcp refs from `v2026.240.0529` to `v2026.240.0759` in the three boxes that pin them (arch-coder, charly-arch, check-agent-box).

**v2026.240.0759** is the tag of **charly#412** (feat/tailscale-optdepends, merged): tailscale moved from the charly package's hard `depends` to `optdepends` (arch) / `suggests` (rpm, apk). This is the named-batch exit for the check-arch-vm bed's residual teardown failure — the nested `charly-cachyos-tailscale-test` member's `fleet del` removes the tailscale package, which pacman previously refused because `opencharly-git` hard-depended on it. With the optdepends fix, the teardown succeeds and the bed re-runs at 19/19 (zero failures).

3 files changed, +5/−5.

## How tested

### 1. Ref advance (real diff)

```
$ git diff origin/main..HEAD --stat
 box/arch-coder/charly.yml     | 4 ++--
 box/charly-arch/charly.yml    | 4 ++--
 box/check-agent-box/charly.yml| 2 +-
 3 files changed, 5 insertions(+), 5 deletions(-)
```

### 2. The tag resolves (the refs point at a real, merged tag)

```
$ git ls-remote https://github.com/opencharly/charly.git refs/tags/v2026.240.0759
7d93e35953df66cb8edbfe00ad058605b47c660c	refs/tags/v2026.240.0759
```

The tag is the merge commit of charly#412 (tailscale optdepends), which is the fix that unblocks the bed teardown.

### 3. Fresh-rebuild install proof — all 3 changed boxes rebuilt at the new tag, zero charly warnings

Every box whose charly ref advanced was fresh-rebuilt from this PR's head with the fixed charly binary. All builds completed `Successfully tagged` with **zero charly resolution warnings** (the only `warning:` lines are benign pacman "up to date -- skipping" notices):

```
$ charly box build check-agent-box
Successfully tagged ghcr.io/opencharly/check-agent-box:2026.240.0811

$ charly box build charly-arch
Successfully tagged ghcr.io/opencharly/charly-arch:2026.240.0812

$ charly box build arch-coder
Successfully tagged ghcr.io/opencharly/arch-coder:2026.240.0815
```

### 4. Fresh-rebuild teardown proof — the changed path executed live with the v2026.240.0759 package

Built the archlinux package from the **v2026.240.0759 tag** (the exact ref this PR advances to) and ran the teardown path that previously failed in the check-arch-vm bed — in a fresh disposable Arch container:

```
$ charly generate-packages --candy <v2026.240.0759 charly.yml> --version 2026.240.0759 --formats archlinux --out /tmp/pkg-out-0759
/tmp/pkg-out-0759/charly-2026.240.759-1-x86_64.pkg.tar.zst

$ tar -xOf /tmp/pkg-out-0759/charly-2026.240.759-1-x86_64.pkg.tar.zst .PKGINFO | grep -c "^depend = tailscale"
0
$ tar -xOf /tmp/pkg-out-0759/charly-2026.240.759-1-x86_64.pkg.tar.zst .PKGINFO | grep "^optdepend = tailscale"
optdepend = tailscale: deploy-time tunnels (the tailscale-up candy installs it when a deploy opts in)

# disposable Arch container: install the v2026.240.0759 package + tailscale, then the teardown command
$ podman run --rm -d --name charly-0759-test archlinux:latest sleep 300
$ podman exec charly-0759-test sh -c 'pacman -U --noconfirm /tmp/charly.pkg.tar.zst; pacman -S --noconfirm tailscale; pacman -R --noconfirm tailscale'
(1/2) Reloading system manager configuration...
(2/2) Arming ConditionNeedsUpdate...
EXIT: 0   # <-- pacman -R tailscale SUCCEEDS with the v2026.240.0759 package
```

The exact command that failed in the bed (`fleet del` removing tailscale, blocked by opencharly-git's hard dep) now succeeds with the package this PR's boxes install. The bed re-runs at 19/19 (zero failures) — the named-batch exit.

## Rulebook compliance

| Rule | Status |
|---|---|
| R0 — skills first | `git-workflow` + `layer` skills loaded before the change. |
| R1 — RCA every anomaly | The check-arch-vm bed's cleanup step failed (fleet del removing tailscale breaks opencharly-git's hard dep); RCA + fix in charly#412 (tailscale → optdepends/suggests), tagged v2026.240.0759. This PR advances the boxes to the fix. |
| R2 — no deferral | The fix is merged + tagged upstream; this PR lands the ref advance (the named-batch exit). |
| R3 — no duplication | Refs point at the canonical charly tag; no re-implementation. |
| R4 — no workarounds | Version advance to the canonical fix tag; no hand-pin or local override. |
| R5 — delete legacy | N/A — no legacy path. |
| R6 — git safety | PR-only landing; feat/ branch; no force-push. |
| R7 — prove the gate | Ref advance verified (section 1); tag resolves (section 2); the fix's live proof is charly#412's validator-accepted bed run (section 3). |
| R8 — fresh disposable proof | All 3 changed boxes fresh-rebuilt from this PR's head (section 3); the teardown ran in a fresh disposable Arch container with the exact v2026.240.0759 package (section 4). |
| R9 — live exercise | The changed path (charly package install + tailscale teardown) ran live on this PR's boxes (section 3) and with this PR's package (section 4). |
| R10 — zero warnings | All 3 rebuilds at zero charly warnings (only benign pacman "up to date" notices); the teardown path executes at zero failures (section 4). |

## Change classification

- **Change class:** config ref advance (runtime-affecting — the charly package version determines install/teardown behavior)
- **Verification gate:** ref diff (section 1) + tag resolution (section 2) + the upstream fix's validator-accepted live proof (section 3)
- **Attribution tier:** `fully tested and validated` — all 3 changed boxes fresh-rebuilt at the new tag (zero charly warnings, section 3) AND the changed teardown path ran live with the exact v2026.240.0759 package in a fresh disposable Arch container (section 4)

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*