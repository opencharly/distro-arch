# 2026.265.1557 — feat(distros): own the arch-family skills in distro-arch

## Summary

Relocate the seven arch-family `skill:` entities from the
`opencharly/layer-charly-distros` aggregator into this repo, which owns the
artifacts they document: `arch` (`box/arch`), `arch-builder`, `arch-aur-test`,
`arch-pac-test`, `arch-test`, `arch-coder` (`box/arch-coder`), `charly-arch`
(`box/charly-arch`). Entities are copied verbatim; only `owner:` is corrected to
each skill's own name, the convention every single-candy repo uses.

Files changed (`git diff --stat origin/main`):

```
 CHANGELOG/2026.264.0042.md | 164 ++++++++++
 charly.yml                 | 771 +++++++++++++++++++++++++++++++++++++++++++++
 2 files changed, 935 insertions(+)
```

`charly.yml` carries the seven entities; `CHANGELOG/2026.264.0042.md` is the
repo's per-release history file (standing convention).

## Change class

**Candy/box config** — `skill:` entity fields in `charly.yml`; the rulebook's
class table states "a YAML field is config". Tier: **`analysed on a live
system`** — the generator (the only consumer of a `skill:` entity) ran live; a
fresh-rebuild bed cannot compose a `skill:` entity and no image/deploy behaviour
changes.

## How tested

**1. `charly box validate` over THIS repo's tree**, with a NEGATIVE CONTROL
proving it actually validates the seven added `skill:` entities. Root and head
are named, and the negative control's error path points at this repo's own
`charly.yml`:

```
# tree: /tmp/opencode/skills-migration/distro-arch
#       (branch feat/own-arch-skills-v2, HEAD 41896234fedadade76f18fa9e4edb3d8778bc85e)
$ charly box validate
charly box validate: OK — checked 132 candies, 11 boxes, 8 deploys, 7 distros, 7 builders; 0 warnings, 0 errors

$ # negative control: drop `name:` from the added `arch-skill` entity, same tree
$ charly box validate
charly: error: command "validate": validation error:
  /tmp/opencode/skills-migration/distro-arch/charly.yml:doc0: node "arch-skill":
  plugin kind:skill: plugin_input fails #SkillInput: #SkillInput.name: incomplete value =~"^[a-z][a-z0-9-]*$"
```

**2. The generator consumed a file BYTE-IDENTICAL to this PR's added entities.**
The scratch candy file was not hand-written: it was produced by extracting the
seven `*-skill` top-level nodes from THIS PR's `charly.yml`, then confirmed
order-independently (each top-level node block compared):

```
$ grep -c '^    skill:$' proj2/candy/distro-arch-skills/charly.yml
7

$ python3 -c '<parse both files into top-level node blocks; compare each>'
scratch nodes:      ['arch-aur-test-skill','arch-builder-skill','arch-coder-skill','arch-pac-test-skill','arch-skill','arch-test-skill','charly-arch-skill']
added-block nodes:  ['arch-aur-test-skill','arch-builder-skill','arch-coder-skill','arch-pac-test-skill','arch-skill','arch-test-skill','charly-arch-skill']
same name set: True
  arch-aur-test-skill      identical=True
  arch-builder-skill       identical=True
  arch-coder-skill         identical=True
  arch-pac-test-skill      identical=True
  arch-skill               identical=True
  arch-test-skill          identical=True
  charly-arch-skill        identical=True
```

**3. Running the real generator over that byte-identical input** emitted the
seven skills (their `SKILL.md` files can only appear if the entities loaded):

```
$ charly marketplace generate --root . --out /tmp/opencode/skills-migration/proof-v2
$ ls proof-v2/distros/skills | grep -E '^(arch|arch-aur-test|arch-builder|arch-pac-test|arch-test)$'
arch
arch-aur-test
arch-builder
arch-pac-test
arch-test
$ ls proof-v2/coder/skills | grep -E '^(arch-coder|charly-arch)$'
arch-coder
charly-arch
```

**4. Byte-identity — the published corpus is unchanged by the move:
`charly marketplace drift` reports the committed corpus matches its sources, and
every moved `SKILL.md` is byte-identical:**

```
$ charly marketplace drift --root . --out /tmp/opencode/skills-migration/proof-v2
marketplace drift: clean (454 artifact(s) on disk match their sources; regeneration is a no-op)

$ diff -q proof-v2/distros/skills/arch/SKILL.md          marketplace/distros/skills/arch/SKILL.md
$ diff -q proof-v2/distros/skills/arch-aur-test/SKILL.md marketplace/distros/skills/arch-aur-test/SKILL.md
$ diff -q proof-v2/distros/skills/arch-builder/SKILL.md  marketplace/distros/skills/arch-builder/SKILL.md
$ diff -q proof-v2/distros/skills/arch-pac-test/SKILL.md marketplace/distros/skills/arch-pac-test/SKILL.md
$ diff -q proof-v2/distros/skills/arch-test/SKILL.md     marketplace/distros/skills/arch-test/SKILL.md
$ diff -q proof-v2/coder/skills/arch-coder/SKILL.md      marketplace/coder/skills/arch-coder/SKILL.md
$ diff -q proof-v2/coder/skills/charly-arch/SKILL.md     marketplace/coder/skills/charly-arch/SKILL.md
# all seven silent = byte-identical
```

Corpus size: `find proof-v2 -type f | wc -l` = **454** total generated files
(333 `SKILL.md` + 13 agents + 27 references + 81 manifests/other). An earlier
draft quoted **427**, which was the same tree counted with `.github/`,
`.claude/`, `candy/`, `tools/` excluded — a scope difference, not a result
difference. 454 is authoritative.

## Landing order (cross-repo wave)

1. **this PR** — merge mints `v<CalVer>`.
2. **`opencharly/marketplace`** — `chore(refs): pin distro-arch:v<CalVer> + regenerate`: adds `@github.com/opencharly/distro-arch:v<CalVer>` to `candy/charly-marketplace/charly.yml` and regenerates; its drift gate proves byte-identity.
3. **`opencharly/layer-charly-distros`** — `refactor(distros): drop the arch-family skills (moved to distro-arch)`: deletes the seven duplicates.

The dual-source window between 1 and 3 is the unavoidable transient of a
repo-split across three independently-landing repos; steps 2–3 land immediately.

## Rulebook compliance

- **R0** — `/charly-internals:skills` + `/charly-internals:marketplace` loaded.
- **R1** — stale `owner:` values and the 427/454 count-scope mismatch corrected here.
- **R2** — in-repo move complete; sibling landings scheduled (2–3).
- **R3** — producer half of de-duplication; step 3 completes it.
- **R4** — no sleeps/retries/workarounds; generator + drift are the gates.
- **R5** — aggregator copies deleted in step 3 of the same wave.
- **R6** — dedicated checkout; PR-only, no force-push.
- **R7** — validate (with negative control) ran over this repo's tree; generate/drift ran over a file byte-identical to the added entities (block-identity proof above).
- **R8/R9** — no labels/plans/schemas/generated files; no binary built here.
- **R10** — config class, `analysed on a live system`; no bed can compose a skill entity.

## Change classification

**Candy/box config** → `analysed on a live system`; proof: validate clean with a
negative control, drift clean (454 artifacts), seven skills byte-identical.

*Assisted-by: opencode ollama-cloud/deepseek-v4.1-flash (analysed on a live system)*