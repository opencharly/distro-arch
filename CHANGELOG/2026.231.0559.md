# 2026.231.0559 — adopt charly v2026.231.0530

CalVer finalized at merge by the pr-validator, and shared with the release tag — consumers pin this
repo by tag, so the two always name the same landing.

Every `@github.com/opencharly/charly/candy/...` pin in this repo moves from `v2026.201.0706` to
`v2026.231.0530` (69 pins across 10 files). This is a pure version adoption: the candies themselves
are unchanged, only the tag they are fetched at.

The adoption is required by the ollama cutover's consumer landing. The `box/cachyos` submodule
composes this repo's arch bases (`arch.arch`, `arch.arch-builder`, `arch.cuda-arch-builder`) AND
pins charly candies directly at `v2026.231.0530` (the tag that first ships `candy/ollama-cuda` and
`candy/ollama-rocm`). With this repo still pinned at `v2026.201.0706`, the resolver saw the shared
candies at two versions and warned once per candy — a warning the zero-warnings R10 gate forbids.
Re-tagging this repo to the same charly tag makes every reference fetch one commit per repo.

The provisional gate is `charly box validate` (clean, exit 0). The authoritative gate is the
`box/cachyos` bed roster against this new tag (B6 step 4) — the consumer's image R10 proves the
composition this repo's pins feed.
