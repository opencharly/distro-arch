# 2026.250.0229 — fix(agent): restore the check-agent-live run-local wiring (git in the sandbox image + the provisioning contract documented)

## Summary

Two-part delta on the agent-control bed stack (the full head-vs-base diff is one coherent unit):

1. **The Cutover C schema migration** — `charly.yml` migrates `2026.240.1943` → `2026.249.2125` and
   unrolls the member-less `group:` node `check-agent-live` into the image-less `agent_provisioned`
   pod spelling (supervisor ruling b): the group carried no members — only the `iterate:` score over
   the SEPARATE `check-agent-pod` sandbox deploy — and the pod spelling keeps the `check-agent-live`
   name addressable for `charly check sync-credential check-agent-live` while the sandbox stays the
   home that exists today. The `group:` spelling is deleted in this same PR (hard cutover).
2. **The run-local wiring fix** the bed-runner host exposed when the re-authored bed was driven end to
   end: `box/check-agent-box` composes `layer-gh` (git + git-lfs + gh) — the in-pod `charly check
   run-local` (plugin-check) clones the bind-mounted `/workspace` per-run and resolves the candy
   `@github` pins via `git ls-remote`; the minimal arch base shipped no git, so every in-sandbox
   harness run died with `exec: "git": executable file not found in $PATH`. The `check-agent-live`
   bed comment now also documents the full harness-sandbox provisioning contract the RCA surfaced.

## How tested

RCA (R1) — the failure chain on the bed-runner host, each layer proven separately:

```
# 1. The premise "the conversion lost wiring" is DISPROVEN:
$ git show 6ee8e99^:charly.yml | sed -n '/check-agent-live:/,/env: {}/p'
   → the original group: node carries a BYTE-IDENTICAL iterate block (sandbox: check-agent-pod);
     plugin-check run_cmd.go dispatchToPod keys off the SANDBOX kind — group vs pod dispatch identically.
   → the original shape NEVER ran on the runner host: cc1-arch-bed.log/2 "no entity check-agent-live"
     (old tree/pin), bed3 "harness sandbox not running", and the pre-migration binary cannot parse the
     migrated tree — the run-local wiring gap is per-host provisioning, not the entity-kind conversion.
# 2. The entity's documented consumer resolves against the re-authored spelling:
$ /tmp/charly-archworker check sync-credential check-agent-live
harness sync-credential: ai=check-agent-live-claude where=pod:check-agent-pod mounts=1   (EXIT=0)
# 3. Schema-migration gate on the final committed tree (92c13b8):
$ /tmp/charly-archworker migrate
already at schema 2026.249.2125; nothing to migrate      (idempotent; git status clean)
$ grep -n 'group:' charly.yml   → only the comment + unix_group: wheel — ZERO group: nodes remain
# 4. Config gate:
$ /tmp/charly-archworker box validate
charly box validate: OK — checked 132 candies, 11 boxes, 8 deploys, 7 distros, 7 builders; 0 warnings, 0 errors
# 5. The rebuilt sandbox image:
$ /tmp/charly-archworker box build check-agent-box        → EXIT=0, tagged ...:2026.250.0200
$ podman run --rm ghcr.io/opencharly/check-agent-box:2026.250.0200 git --version
git version 2.55.0
# 6. The bed R10 (AI-iterate loop, plateau 1, the disposable sandbox freshly preflighted per run):
$ timeout 1800 /tmp/charly-archworker check run check-agent-live
harness: preflight restart of disposable harness sandbox "check-agent-pod" (fresh-per-run)
harness sync-credential: ai=check-agent-live-claude where=pod:check-agent-pod mounts=1
harness: score=check-agent-live ai=check-agent-live-claude run=20260907-022328-4fa6b4 where=pod:check-agent-pod
harness: iter 1 end → solved 0 this iter; cumulative 0/0
harness: score=check-agent-live ai=check-agent-live-claude exit=plateau iterations=1 best=0/0
  result: .check/check-agent-live/results/result-2026.250.0224.yml
  branch: charlycheck/20260907-022328-4fa6b4
```

The run-local wiring is PROVEN live end to end: preflight restart → host-binary sync → credential
sync → in-pod project resolve (44-repo git metadata fetch) → candy-layer downloads → per-run clone of
the bind-mounted /workspace → plan resolve → AI invocation → scoring → plateau exit → result file +
branch push-back. The AI runner itself failed at the PROVIDER credential layer, not the wiring:
`Your organization has disabled Claude subscription access for Claude Code · Use an Anthropic API key
instead` (iter1/runner.log, retained in the run artifacts) — an operator-credential dependency (no
ANTHROPIC_API_KEY exists on the bed-runner host), unchanged by this PR. Residual host-infra findings
from the same campaign (out of this repo's scope): the stale Sep-3 rootless-netns pasta broke ALL
rootless container egress (ENETUNREACH; fixed by respawning it — containers now reach GitHub), and the
per-host sandbox overlay entry had lost `disposable: true` in the fleet-add merge (restored operator-side).

# 7. The R10 driver `check run check-agent-pod` re-proven from the settled CC-1 gate binary
     (charly feat/drop-group-kind @ b6d9057b + re-pin 83da3dfa, `task build:binary` → 2026.250.0215,
     carries the compiled-in-plugin-schema gate fix charly #551 / 35358d97):
$ timeout 1800 charly check run check-agent-pod
[image-build]         PASS after 26.753s  (allowlisted=1 cache=170/184)
[check-image]         PASS after 26.276s  (allowlisted=1)   ← the schema-gate class is fixed in-binary
[deploy-add]          PASS after 1.942s   (allowlisted=1)
[config]              PASS after 2.635s   (allowlisted=1)
[start]               PASS after 1.972s   (allowlisted=1)
[bring-up-members]    PASS after 25ms
[check-live]          PASS after 1m4.243s (allowlisted=1)
[feature-run]         PASS after 15.903s  (allowlisted=1)
[update]              PASS after 11.451s  (allowlisted=5)
[check-live-rebuild]  FAIL after 1m12.263s — 109 steps: 84 passed, 1 failed, 24 skipped

The single failing check is `native-pi-two-process-resume`:
  mktemp: failed to create directory via template '/home/user/.pi/agent/sessions/r10.XXXXXX': Permission denied
RCA: the pi-agent candy's named volume `sessions` (~/.pi/agent/sessions) is re-created ROOT-OWNED by
the `charly update` destroy+rebuild (pod volume copy-up): `podman exec charly-check-agent-pod ls -la
/home/user/.pi/agent/sessions` → `drwxr-xr-x 2 root root` stamped at the update time, while the same
step PASSes on the pre-update `check-live` pass. The identical volume declaration exists in the
pre-branch candy (v2026.231.0530) and this delta does not touch `check-agent-pod`, the box's volume
surface, or the pi-agent candy — pre-existing update/volume substrate behavior, root fix belongs in
the volume-copy-up owner, not this repo.

Upstream finding for the CC-1 charly half: plugin-check #30 (stopped-root live-gather) is MERGED but
its nested-module tag `candy/plugin-check/v0.2026250.0150` was never pushed (only the repo-root tag
`v2026.250.0150`; `git ls-remote --tags` audited), so the module proxy cannot resolve it — the
charly pin bump to the fix needs that tag pushed first. Proven via a disposable /tmp build with a
local `replace`: with the fix, `check-live-rebuild` executes for real (1m12s, 108/109) instead of
the 1.8s `container charly-check-agent-pod is not running` hard-fail.

## Rulebook compliance

- **R0 (skills first)** — /charly-distros:arch, /charly-image:layer, /charly-internals:git-workflow +
  the umbrella rulebook loaded before the first edit; plugin-check / plugin-deploy-pod sources read for
  the RCA.
- **R1 (RCA every anomaly)** — each failure layer (no /workspace → no git → stale schema/binary →
  submodule gitfile → org-blocked credential → the UNSTAMPED-binary gate → the shared-/tmp binary
  overwrite race) traced to its mechanism before remediation; the parent-orchestrator premise
  ("the original had wiring to restore") was DISPROVEN with evidence, not patched around.
- **R2 (report, don't classify away)** — the AI-leg credential block and the two host-infra findings are
  reported above with retained output, not classified out of scope.
- **R3 (no duplication)** — git arrives via the canonical `layer-gh` candy (the single git-shipping
  implementation), not a hand-rolled package list; the provisioning contract is documented once, on the
  bed, mirroring the clone.go contract header.
- **R4 (no workarounds)** — no sleeps/retries/magic values; the pasta respawn was an RCA'd
  root-cause repair of the runner host, not a bed bypass; the bed run uses the stamped binary the
  freshness gate demands.
- **R5 (delete legacy)** — the `group:` spelling is DELETED in this same delta (hard cutover):
  `grep -n 'group:' charly.yml` shows only a comment and the unrelated `unix_group: wheel` — zero
  `group:` nodes remain; `charly migrate` is idempotent at the new schema (nothing to migrate). The
  replacement pod spelling is not a supplement — the old node does not exist anywhere in the tree.
- **R6 (git safety)** — feature branch `feat/drop-group-kind`, no force-push, no hook bypass, PR-only.
- **R7 (prove the gate, not the plan)** — `charly box validate` + the `charly migrate` idempotence
  check pasted on the final committed tree; the bed run matrix pasted with retained artifact paths
  (result yml + pushed charlycheck branch).
- **R8 (one writer per cwd)** — all edits in this repo's single worktree; the shared /tmp binary race
  from a sibling worker was hit and neutralized with a worker-unique stamped binary copy.
- **R9 (pin discipline)** — the new `@github.com/opencharly/layer-gh:v2026.239.1624` ref is a MERGED
  default-branch tag (the same pin charly-arch already composes); no PR-branch pins.
- **R10 (fresh disposable proof)** — the bed ran against a fresh per-run clone of the committed head
  (92c13b8) inside the freshly-rebuilt disposable sandbox, per the harness's own fresh-proof mechanism.

- **R9 (binary equals source)** — every gate binary is the CalVer-stamped `task build:binary`
  product of its own worktree (2026.250.0215 / the worker-unique copies above), never a shared
  installed binary; the /tmp `replace` proof build is equally stamped and disposable.
- **R10 (fresh disposable proof)** — the exact R10 gates ran fresh from the final committed tree:
  the `check-agent-live` driver (#6) and the full `check-agent-pod` disposable bed rebuild (#7,
  10-step matrix retained in `.check/check-agent-pod/2026.250.0222/`).
## Change classification

- **Change class:** schema migration (`charly.yml` version bump `2026.240.1943` → `2026.249.2125` +
  the `check-agent-live` entity-kind change `group:` → `pod:` `agent_provisioned`) PLUS a test-bed
  fix (the `layer-gh` composition into `check-agent-box` + bed-comment documentation). No product
  paths; no candy pins moved (the one added `@github` ref is a new composition member, not a pin bump).
- **Verification gate:** the schema-migration gate — `charly box validate` (0 warnings, 0 errors) +
  `charly migrate` idempotence (already at 2026.249.2125; nothing to migrate) on the final committed
  tree — plus the test-bed gate: the live `check sync-credential` / `check run check-agent-live`
  matrix pasted above.
- **Attribution tier:** `analysed on a live system` — every changed path executed live (migrated tree
  validated + migrate-idempotent; the sandbox image rebuilt and run in-pod; the run-local chain driven
  end to end with retained output), but the bed's AI leg stops at the provider-credential wall
  (org-disabled subscription access, no API key on the host), so the full AI-scoring pass is not claimed.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*