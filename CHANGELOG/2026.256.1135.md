# 2026.256.1135 — docs: correct arch box README

## Summary

Correct stale facts in the repo-root `README.md` — the arch box README. (The
requested `box/arch/README.md` does not exist in this repo; the box README is the
repo-root `README.md`, where every described stale fact lives.)

- Candy refs moved out of `opencharly/charly/candy/*` into standalone repos; the
  README now cites the real
  `@github.com/opencharly/<layer-*|pod-*|plugin-*>[:subdir]:<tag>` shape.
- Candy refs pin to many explicit CalVer tags, not "a single tag".
- The schema-version hard-fail message is `"newer than this charly supports"`,
  not the quoted "update charly".
- `.github/workflows/auto-merge.yml` does not exist in this repo; the citation is
  corrected to the real files (`.github/workflows/pr-validator.yml`,
  `.github/workflows/tag-on-merge.yml`, `.github/scripts/auto-merge-*.sh`).
  **No landing rule is changed**: the "Landing & releases" numbered rules and the
  "engine is the sole merge executor" closing line are restored verbatim from
  `origin/main`; only the dead filename in the citation changed.

## How tested

Documentation-only change; no runtime gate. Every correction was verified against
the live tree and the claim-keyed sweeps were run over the whole repo.

Stale candy-ref shape and "single tag" claim — CHANGELOG history only, no live
`*.yml` refs:

```
$ grep -rn 'charly/candy' --include='*.md' .
./CHANGELOG/2026.241.1429.md:5:The candy de-submodule cutover moved these candies out of `charly/candy/` into their own
./CHANGELOG/2026.241.1429.md:7:(`@github.com/opencharly/charly/candy/<name>:<charly tag>`).
./CHANGELOG/2026.241.1429.md:12:| was `charly/candy/…` | now |
./CHANGELOG/2026.241.1429.md:60:$ grep -rn '@github.com/opencharly/charly/candy/' --include='*.yml' .
./CHANGELOG/2026.240.0720.md:5:The candy de-submodule cutover (Phase-4 residue sweep) for the Arch image family: every candy ref in this repo's `charly.yml` files advances from the in-repo `@github.com/opencharly/charly/candy/<name>:v2026.231.0530` pins to the standalone kind-prefixed repos (`layer-*`, `pod-*`, `plugin-*/candy/*`), and the two post-cutover tag advances land:
./CHANGELOG/2026.240.0720.md:118:| R5 — delete legacy | The old `charly/candy/<name>` pins are fully removed from this repo's tree. |
./CHANGELOG/2026.231.0559.md:6:Every `@github.com/opencharly/charly/candy/...` pin in this repo moves from `v2026.201.0706` to
exit=0

$ grep -rn 'charly/candy' --include='*.yml' .
exit=1

$ grep -rn 'single tag' .
exit=1
```

Full `@github` ref enumeration over every box candy list — no `head`, so the prose
enumeration `<layer-*|pod-*|plugin-*>[:subdir]` is demonstrably exhaustive
(distinct refs: 48; no `vm-*` or `distro-*` refs appear):

```
$ grep -rhoE '@github\.com/opencharly/[A-Za-z0-9_./-]+:[A-Za-z0-9._-]+' charly.yml box/*/charly.yml | sed 's#/.*@#@#' | sort -u
@github.com/opencharly/layer-agent-forwarding:v2026.243.0408
@github.com/opencharly/layer-asciinema:v2026.240.0119
@github.com/opencharly/layer-build-toolchain:v2026.239.1636
@github.com/opencharly/layer-charly:v2026.243.0408
@github.com/opencharly/layer-claude-code:v2026.239.1626
@github.com/opencharly/layer-codex:v2026.243.0408
@github.com/opencharly/layer-container-nesting:v2026.239.1647
@github.com/opencharly/layer-cuda:v2026.243.0408
@github.com/opencharly/layer-devops-tools:v2026.243.0408
@github.com/opencharly/layer-dev-tools:v2026.239.1624
@github.com/opencharly/layer-direnv:v2026.242.1147
@github.com/opencharly/layer-docker-ce:v2026.239.1626
@github.com/opencharly/layer-forgecode:v2026.243.0408
@github.com/opencharly/layer-gemini:v2026.243.0409
@github.com/opencharly/layer-gh:v2026.239.1624
@github.com/opencharly/layer-github-actions:v2026.242.1323
@github.com/opencharly/layer-golang:v2026.239.1614
@github.com/opencharly/layer-google-cloud-npm:v2026.243.0515
@github.com/opencharly/layer-google-cloud:v2026.241.0656
@github.com/opencharly/layer-grafana-tools:v2026.239.1628
@github.com/opencharly/layer-kubernetes:v2026.241.0654
@github.com/opencharly/layer-language-runtimes:v2026.243.0409
@github.com/opencharly/layer-nodejs:v2026.239.1624
@github.com/opencharly/layer-nvenc-headers:v2026.239.1627
@github.com/opencharly/layer-nvidia:v2026.236.116
@github.com/opencharly/layer-oracle:v2026.243.0409
@github.com/opencharly/layer-pacstrap-builder:v2026.239.1633
@github.com/opencharly/layer-pi-agent:v2026.239.1638
@github.com/opencharly/layer-pixi:v2026.239.1634
@github.com/opencharly/layer-pre-commit:v2026.243.0409
@github.com/opencharly/layer-ripgrep:v2026.235.1653
@github.com/opencharly/layer-rust:v2026.239.1627
@github.com/opencharly/layer-supervisord:v2026.240.0121
@github.com/opencharly/layer-tailscale-up:v2026.243.0410
@github.com/opencharly/layer-tailscale:v2026.240.0121
@github.com/opencharly/layer-tmux:v2026.240.0202
@github.com/opencharly/layer-typst:v2026.240.1854
@github.com/opencharly/layer-uv:v2026.239.1631
@github.com/opencharly/layer-vscode:v2026.239.1631
@github.com/opencharly/layer-yay:v2026.240.0121
@github.com/opencharly/plugin-agent/candy/plugin-agent:v2026.242.0525
@github.com/opencharly/plugin-agent-pi/candy/plugin-agent-pi:v2026.242.0525
@github.com/opencharly/plugin-spice/candy/plugin-spice:v2026.242.0533
@github.com/opencharly/plugin-tmux/candy/plugin-tmux:v2026.242.0534
@github.com/opencharly/pod-charly-mcp:v2026.243.0516
@github.com/opencharly/pod-check-keepalive:v2026.239.1625
@github.com/opencharly/pod-dbus:v2026.243.1831
@github.com/opencharly/pod-sshd:v2026.239.1637
distinct: 48
```

The three cited landing files are real (the old `auto-merge.yml` citation was not):

```
$ ls .github/workflows .github/scripts
.github/scripts:
auto-merge-harness.sh
auto-merge-runblock.sh

.github/workflows:
pr-validator.yml
tag-on-merge.yml
```

The schema hard-fail quote is the live source string (real source path is
`opencharly/sdk`, not the speculative `spec/loaderkit`):

```
$ grep -rn 'newer than this charly supports' /home/atrawog/Sync/Atrapub/oo/opencoder/opencharly/sdk/loaderkit/load_unified.go
83:			"%s: config schema %s is newer than this charly supports (max %s). Update charly (reinstall the latest opencharly package, or run 'task build:binary' from a fresh checkout and use ./bin/charly)",
```

## Rulebook compliance

- **R1** — every divergence was root-caused (candy de-submodule cutover; workflow
  cutover) before rewriting; no "pre-existing" classification.
- **R2** — the whole cutover is finished: the claim-keyed sweep above covers every
  in-scope surface (README prose + all `*.yml` candy refs + `single tag`), with
  only CHANGELOG history remaining.
- **R3 — no duplication** — documentation-only; the corrected ref shape and the
  landing narrative are each stated once, not duplicated across files.
- **R4** — N/A — documentation-only; no sleeps, retries, suppressions, or
  fallback branches are introduced.
- **R4a — binary-only commands** — every command the README tells a reader to run
  (`charly box build`, `charly -C box/arch box build`, `charly --repo
  opencharly/distro-arch box build`) works with only the `charly` binary; no
  clone, `cd`, or `task` target is required.
- **R5** — the stale ref shape and the dead `auto-merge.yml` citation are removed
  completely (not shimmed); the sweep above is the self-test.
- **R6** — edits were made in the fresh `/tmp/opencode/distro-arch-refs`
  worktree of the submodule (never in the umbrella checkout); `git status` was
  clean before editing; landing is PR-only, no force-push, no direct push to
  `main`.
- **R7** — N/A — documentation-only; no runtime behavior changed, so no test can
  fail without the change.
- **R8** — N/A — documentation-only; no labels, plans, configs, schemas, or
  generated files are emitted or changed.
- **R9** — N/A — documentation-only; no binary is built or invoked here.
- **R10** — N/A — documentation-only; no image is generated or deployed and no
  bed is invented (R10's docs clause).

## Change classification

documentation-only → `documentation reviewed` (the whole diff is markdown; no pins
or scripts changed). Materially the arch README only.

*Assisted-by: opencode deepseek-v4.1-flash (documentation reviewed)*